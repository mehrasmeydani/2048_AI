[project]
name = "agent2048"
version = "0.1.0"
description = "2048 RL training and inference workspace"
requires-python = ">=3.11"

[tool.setuptools]
package-dir = {"" = "src/python"}

[tool.setuptools.packages.find]
where = ["src/python"]
